package com.feuerschvenger.perlinsedge.domain.utils;

/**
 * Enum representing the different game modes in Perlin's Edge.
 * This is used to manage the current state of the game and control various UI elements.
 */
public enum GameMode {
    EXPLORATION,
    BUILDING_MODE,
    INVENTORY_OPEN,
    CRAFTING_OPEN,
    PAUSED,
    // Add other modes as needed (e.g., DIALOGUE, COMBAT)
}